version: 0.2

phases:
  build:
    commands:
      - echo "===============" && ps -ef
      - echo "===============" && env
      - echo "===============" && ls -l
      - echo "===============" && pwd
      - echo "====Execute====" && cd cmd && terraform init && terraform get && terraform plan
    